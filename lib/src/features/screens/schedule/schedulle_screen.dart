import 'package:flutter/material.dart';

class SchedulleScreen extends StatelessWidget {
  const SchedulleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
